// Shared by the pre-paint boot script (src/lib/boot-script.ts) and the client code.
export const PREFERENCES_KEY = "outlaw-journal:preferences";
/** sessionStorage: set on the first page of a visit, so the loader shows once per session. */
export const VISITED_KEY = "outlaw-journal:visited";
